# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** — Legacy JSON + Knowledge Drift
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate both bug forms exist in the workspace
  - **Scoped PBT Approach**: Scope to concrete failing cases in the current workspace
  - Form A (JSON format split): Check `agent-memory/**/*.json` — expect to find `date-index.json`, `keyword-index.json`, `index.json`, `toolingLessonsIndex.json`
  - Form B (Knowledge drift): Compare `palace/search-index.md` session dates against `knowledge/evolution.md` Change Log dates — sessions saved without knowledge updates show gaps
  - Verification checklist (run on UNFIXED workspace):
    - `ls agent-memory/palace/date-index.json` → exists (BUG)
    - `ls agent-memory/palace/keyword-index.json` → exists (BUG)
    - `ls agent-memory/knowledge/index.json` → exists (BUG)
    - `ls agent-memory/knowledge/lessons/tooling/toolingLessonsIndex.json` → exists (BUG)
    - Count `.json` files under `agent-memory/` → >0 (BUG)
    - `references/adaptation.md` Step 4 shows JSON index creation (BUG)
  - Run on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (confirms bugs exist — JSON files found, knowledge drift detected)
  - Document counterexamples found to understand root cause
  - Mark task complete when checks are run and failures documented
  - _Requirements: 1.1, 1.2, 1.3, 1.6, 1.7, 2.1, 2.2, 2.3, 2.10_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** — Palace Structure + Knowledge Format Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe on UNFIXED code and record current state:
    - Palace structure: `wings/*/hall.md`, `rooms/*.md`, `closets/*.md` exist with correct format
    - Knowledge articles: `design-craftsmanship-tokens.md` and `error-recovery-strategy.md` have YAML frontmatter intact
    - Lesson details: `LESSON-TOOLING-001.md` through `LESSON-TOOLING-004.md` have `---` frontmatter + Summary/Detail/Apply/Evidence sections
    - `tunnels.md` table format and content unchanged
    - `state.md` sections (Active Wings, Recent Sessions, Current Focus, Open Threads) present
    - `search-index.md` existing rows present and correctly formatted
    - `user-profile.md` content intact
    - `archive/` directory structure intact
  - Write preservation checklist capturing observed state:
    - Record file count in each palace directory
    - Record frontmatter fields of each knowledge article
    - Record lesson file count and IDs
    - Record `tunnels.md` row count
    - Record `state.md` section headers
    - Record `search-index.md` row count
  - Run on UNFIXED code
  - **EXPECTED OUTCOME**: All preservation checks PASS (confirms baseline behavior to preserve)
  - Mark task complete when checks are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8_

- [x] 3. Phase 1 — Core Fix: Workspace Migration + JSON Cleanup

  - [x] 3.1 Delete legacy JSON files from workspace
    - Delete `agent-memory/palace/date-index.json` (empty `{}`, replaced by `search-index.md`)
    - Delete `agent-memory/palace/keyword-index.json` (empty `{}`, replaced by `search-index.md`)
    - Delete `agent-memory/knowledge/index.json` (stale hardcoded data, replaced by `knowledge/index.md`)
    - Delete `agent-memory/knowledge/lessons/tooling/toolingLessonsIndex.json` (replaced by `lessons/tooling/index.md`)
    - Verify no unique data in JSON files that isn't already in Markdown equivalents
    - Confirm zero `.json` files remain under `agent-memory/`
    - _Bug_Condition: isBugCondition(saveOp) where jsonExists = true for agent-memory/**/*.json_
    - _Expected_Behavior: Zero .json files under agent-memory/ after deletion_
    - _Preservation: No Markdown files modified or deleted during this step_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 2.10_

  - [x] 3.2 Verify Markdown knowledge files exist and match schemas
    - Verify `knowledge/index.md` exists with Articles table (ID, Type, Scope, Status, Score, Updated, Path, Keywords)
    - Verify `knowledge/index.md` exists with Lessons table (ID, Domain, Type, Status, Applied, Prevented, Updated, Path)
    - Verify `knowledge/index.md` exists with Gaps table (Domain, Gap, First Seen, Status, Notes)
    - Verify `knowledge/evolution.md` exists with Change Log table (Date, ID, Change, Signal, Before, After, Evidence)
    - Verify `lessons/tooling/index.md` exists with correct columns per `references/storage.md`
    - Confirm all tables are parseable as Markdown tables (pipe-delimited, header row + separator)
    - _Bug_Condition: JSON files were authoritative; now Markdown must be verified as complete replacement_
    - _Expected_Behavior: All knowledge files match references/storage.md schemas_
    - _Preservation: File content unchanged — only verifying, not modifying_
    - _Requirements: 2.2, 2.3, 2.6, 2.8_

  - [x] 3.3 Verify session-load and session-save hooks work after JSON removal
    - Confirm session-load hook v3.0.0 reads `knowledge/index.md` and `knowledge/evolution.md` (not JSON)
    - Confirm session-load bootstrap creates Markdown files only if missing
    - Confirm session-save hook v6.0.0 Step 4 (Knowledge Sync Gate) writes to Markdown knowledge files
    - Confirm session-save hook v6.0.0 Step 5 verifies no JSON was written
    - Confirm minimal safe save fallback updates `knowledge/index.md` Updated date + `evolution.md` audit row
    - No hook file changes needed — hooks are already correct (v6.0.0 save, v3.0.0 load)
    - _Bug_Condition: Hooks must not reference or depend on deleted JSON files_
    - _Expected_Behavior: Hooks operate correctly with Markdown-only workspace_
    - _Preservation: Hook versions unchanged (v6.0.0 save, v3.0.0 load)_
    - _Requirements: 1.4, 1.5, 2.4, 2.5_

  - [x] 3.4 Update references/adaptation.md Step 4 to show Markdown instead of JSON
    - Replace JSON index creation example in Step 4 with Markdown table creation matching `references/storage.md` Knowledge Index format
    - Update Global Knowledge Structure section to show Markdown paths (`knowledge/index.md`, not `index.json`)
    - Verify `references/maintenance.md` examples reference Markdown files (already has legacy note header)
    - _Bug_Condition: Reference files showing JSON examples cause agents to create JSON files_
    - _Expected_Behavior: All reference examples show Markdown-only patterns_
    - _Preservation: Reference file structure and other steps unchanged_
    - _Requirements: 1.6, 2.6, 2.10_

  - [x] 3.5 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** — Zero JSON + Knowledge Sync
    - **IMPORTANT**: Re-run the SAME checks from task 1 — do NOT write new checks
    - The checks from task 1 encode the expected behavior
    - When these checks pass, it confirms the expected behavior is satisfied
    - Re-run: `find agent-memory/ -name "*.json"` → 0 results
    - Re-run: `references/adaptation.md` Step 4 shows Markdown, not JSON
    - **EXPECTED OUTCOME**: All checks PASS (confirms bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.10_

  - [x] 3.6 Verify preservation tests still pass
    - **Property 2: Preservation** — Palace Structure + Knowledge Format Unchanged
    - **IMPORTANT**: Re-run the SAME checks from task 2 — do NOT write new checks
    - Re-run all preservation checks from task 2
    - Confirm palace structure, knowledge articles, lessons, tunnels, state, search-index, user-profile, archive all unchanged
    - **EXPECTED OUTCOME**: All preservation checks PASS (confirms no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 4. Phase 2 — Search Index + Score-Based Routing + User Profile

  - [x] 4.1 Add search-index.md archival mechanism
    - Implement archival: rows >180 days old move to `palace/archive/search-index-archive.md`
    - Enforce hard cap: max 500 rows in active `search-index.md`
    - Add consolidation trigger check at maintenance time
    - Document archival format in `search-index-archive.md` (same table schema as `search-index.md`)
    - _Bug_Condition: Unbounded search-index.md grows indefinitely, wasting tokens_
    - _Expected_Behavior: Active index stays ≤500 rows; old rows archived_
    - _Preservation: Existing search-index.md rows unchanged; only old rows moved_
    - _Requirements: 2.7_

  - [x] 4.2 Add score-based knowledge routing to session workflow
    - Extract intent from user request during session-load
    - Scan `knowledge/index.md` for matching keywords and article titles
    - Scan `knowledge/lessons/{domain}/index.md` for relevant lessons
    - Prefer higher score (7.0+ = proven, 3.0–6.9 = active, <3.0 = stale)
    - Record gaps in `knowledge/index.md` Gaps table when nothing matches
    - Add routing logic as guidance in session-load hook or as a reference file agents follow
    - _Bug_Condition: Sessions start without leveraging existing knowledge_
    - _Expected_Behavior: Relevant knowledge surfaced at session start based on score_
    - _Preservation: Knowledge index format unchanged; only read during routing_
    - _Requirements: 2.5, 2.6_

  - [x] 4.3 Add user-profile.md enhancements
    - Add nudge skip tracking section (count of skipped nudges per category)
    - Add preferred save sensitivity setting (how aggressively to capture lessons)
    - Add domain preferences for routing (which knowledge domains to prioritize)
    - _Bug_Condition: User preferences not tracked, leading to generic behavior_
    - _Expected_Behavior: User profile captures nudge, save, and routing preferences_
    - _Preservation: Existing user-profile.md content unchanged; new sections appended_
    - _Requirements: 3.6_

- [x] 5. Phase 3 — Skill Crystallization + Consolidation + Nudges

  - [x] 5.1 Implement skill crystallization
    - Detect patterns appearing 2+ times with positive outcomes in `knowledge/evolution.md`
    - Create draft article in `knowledge/{pattern-id}.md` with YAML frontmatter (status: draft)
    - Add to `knowledge/index.md` Articles table as `draft` status
    - Promote to `active` after one more positive use
    - Promote to `proven` at score ≥7.0 with multiple positive uses
    - Safeguard: verify both source sessions had same intent before crystallizing (GOTCHAS #23)
    - Always ask user confirmation before creating draft article
    - _Bug_Condition: Repeated patterns never captured as reusable knowledge_
    - _Expected_Behavior: Patterns auto-detected and crystallized into knowledge articles_
    - _Preservation: Existing knowledge articles unchanged; only new drafts added_
    - _Requirements: 1.9, 2.5, 2.9_

  - [x] 5.2 Implement consolidation/auto-dream
    - Trigger conditions (any one): sessions_since_consolidation ≥5, days_since ≥7, knowledge items +20%, user request
    - Step 1: Deduplication — group lessons by semantic similarity, merge duplicates
    - Step 2: Stale detection — flag items not used in 90+ days
    - Step 3: Date normalization — replace relative dates with absolute
    - Step 4: Conflict resolution — flag contradictions, resolve with human
    - Step 5: Score normalization — recalibrate if >80% items ≥7.0 (quarterly max)
    - Step 6: Auto-dream — distill settled facts per domain thresholds (arch:5, api:4, ui:2, default:3 sessions unchanged)
    - Step 7: Archive source rooms that fed into distilled statements
    - Step 8: Update tracking — reset sessions_since_consolidation, log summary
    - _Bug_Condition: Knowledge accumulates without distillation, growing stale and redundant_
    - _Expected_Behavior: Periodic consolidation keeps knowledge fresh and deduplicated_
    - _Preservation: Non-stale, non-duplicate knowledge unchanged_
    - _Requirements: 1.10, 2.8, 2.9_

  - [x] 5.3 Implement nudge system
    - At session start, show max 3 nudges from: stale knowledge, open gaps, repeated lessons, old open threads
    - Filter nudges by relevance to today's task (keyword match against user request)
    - Suppress nudges user ignored recently (read skip counts from `user-profile.md`)
    - If skip rate >80% → reduce to max 2 nudges, show High priority only
    - Format nudges as brief actionable items at session-load output
    - _Bug_Condition: Agents start sessions without awareness of relevant stale/gap items_
    - _Expected_Behavior: Max 3 relevant nudges shown at session start_
    - _Preservation: Session-load output format unchanged except for nudge section appended_
    - _Requirements: 2.5_

- [x] 6. Checkpoint — Ensure all phases verified
  - Re-run bug condition checks: zero JSON files, knowledge syncs with palace on every dirty save
  - Re-run preservation checks: palace structure, knowledge format, lessons, tunnels, state all intact
  - Verify Phase 2 features: search-index archival works, routing surfaces relevant knowledge, user-profile tracks preferences
  - Verify Phase 3 features: crystallization detects patterns, consolidation triggers correctly, nudges appear at session start
  - Ensure all tests pass, ask the user if questions arise
