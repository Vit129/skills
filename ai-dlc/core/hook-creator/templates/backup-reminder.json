{
  "name": "Backup Reminder on Data Change",
  "version": "1.0.0",
  "description": "When raw data files are edited, remind user to run backup.",
  "when": {
    "type": "fileEdited",
    "patterns": ["src/data/raw/**/*.js", "src/data/raw/**/*.json"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Raw portfolio data was just modified. Remind the user to run `npm run backup` to save a snapshot. Check if the last backup in backups/ is from today — if not, suggest running it now. Keep the message short."
  }
}
