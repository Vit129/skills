{
  "name": "Run Tests After Task Completes",
  "version": "1.0.0",
  "description": "After a spec task is marked completed, run the full test suite to verify nothing broke.",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "cd [TEST_ROOT] && npx cross-env LANG=th ENV=sit npx playwright test --config=playwright.config.ts --reporter=line"
  }
}
