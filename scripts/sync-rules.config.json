{
  "source": "~/.claude/rules",
  "targets": {
    "codex": {
      "enabled": true,
      "path": "~/.codex/rules",
      "description": "OpenAI Codex CLI \u2014 mirror all Claude rules into ~/.codex/rules/",
      "mode": "mirror",
      "exclude": [
        "*.DS_Store"
      ]
    },
    "gemini": {
      "enabled": true,
      "path": "~/.gemini/rules",
      "description": "Google Gemini CLI \u2014 mirror all Claude rules into ~/.gemini/rules/",
      "mode": "mirror",
      "exclude": [
        "*.DS_Store"
      ]
    }
  },
  "version": "1.2.0"
}