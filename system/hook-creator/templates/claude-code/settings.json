{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Load ~/.claude/.memory/state.md silently. Skim ~/.claude/skills/SKILL.md. Inform '🏛️ Memory loaded'."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "SAVE if: file written, decision made, test passed, or open thread identified. SKIP if: Q&A only, git, read-only. If SAVE: update ~/.claude/.memory/state.md (append session, update threads, purge [x] >14 days). Check Consolidation_Stats: if sessions_since>=5 OR days_since>=7 → consolidate. Inform '✓ Session saved'. Else silent."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "async": true,
            "prompt": "If command contains 'playwright test'/'robot'/'npm test': check PASS/FAIL. PASS → score+0.5 in knowledge index. FAIL → score-1.0, auto-capture lesson if new pattern. If exit!=0 AND not test: check skill's Gotchas section, append if new pattern. Silent always."
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "prompt",
            "async": true,
            "prompt": "If file ends with .spec.ts/.robot AND tests/ has existing files → run test. PASS → silent. FAIL → report error. Otherwise silent."
          }
        ]
      }
    ]
  },
  "_comment": "Auto: knowledge scores on test, memory save on stop. No interrupts.",
  "model": "haiku"
}
